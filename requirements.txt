# Requires Python 3.10+
# Web framework + server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Google ADK — agent framework with LiveRequestQueue and run_live()
# google-genai unpinned — ADK pulls the version it needs
google-adk>=0.5.0
google-genai>=1.12.1

# WebRTC — peer connection, ICE, DTLS, SRTP. Replaces WebSocket for audio.
# Windows: needs Visual C++ build tools → https://visualstudio.microsoft.com/visual-cpp-build-tools/
# Linux/Cloud Run: installs cleanly
aiortc>=1.9.0
aiohttp>=3.9.0      # used by aiortc internally

# Supabase — Postgres + Storage client
supabase>=2.3.0

# Env vars in local dev
python-dotenv>=1.0.0

# MCP protocol — each mcp_server uses this
mcp>=1.2.0
