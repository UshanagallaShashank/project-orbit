// Orchestration tab: live agent graph and error visibility
import { EmptyState } from "../EmptyState";

export function OrchestrationTab() {
  return (
    <EmptyState
      title="Agent graph coming soon"
      hint="A live React Flow graph of every agent run, with errors surfaced in full context. Lands in phase 8."
    />
  );
}
